Releasing unmidl:

Goes through an executable and gets the IDL file from it that MIDL compiled into it.

To install, just unzip inside your Immunity Debugger root directory.


unmidl pycommand is released under GPLv2.0

Dami